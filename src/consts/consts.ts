export interface IMenuName {
  HOME: string;
  PROJECTS: string;
  ABOUT: string;
  CONTACT: string;
}

export const MenuName: IMenuName = Object.freeze({
  HOME: 'home',
  PROJECTS: 'projects',
  ABOUT: 'about',
  CONTACT: 'contact',
});
